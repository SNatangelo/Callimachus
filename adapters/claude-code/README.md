# Claude Code Adapter

The `SKILL.md` file lives at the **repository root** (not here): Claude
Code discovers a skill only if `SKILL.md` is at the root of its folder.

Plug-and-play installation:

```bash
cp -r citation-verifier ~/.claude/skills/citation-verifier
# or, for a repo: symlink in .claude/skills/ of the project
```
